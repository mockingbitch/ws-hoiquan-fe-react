import * as constants from '../Constants';

const initState = {
    user: [],
    token: ''
}

const reducer = (state, action) => {
    // switch (action.type) {
    //     case 'SET_USER':
    //         return {
    //             ...state,
    //             user: action.user
    //         }
    //     default:
    //         return state
    // }
}

export {initState}
export default reducer;